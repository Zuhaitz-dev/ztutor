#ifndef LINUX_SPINLOCK_API_UP_H
#define LINUX_SPINLOCK_API_UP_H

/*
** Authentic Linux Kernel UP (Uniprocessor) implementation.
** Source: include/linux/spinlock_api_up.h
*/

// 2. TODO: Define the spin_lock(lock) macro for single-core devices.
// It must evaluate the lock variable to suppress "unused variable" warnings,
// but generate absolutely zero machine code.
// Use the classic kernel trick: do { (void)(x); } while(0)



#endif /* LINUX_SPINLOCK_API_UP_H */
